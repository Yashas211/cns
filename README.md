
SecureVault Ultimate (FINAL)

Backend:
cd backend
pip install -r requirements.txt
python -m uvicorn main:app --reload

Frontend:
cd frontend
npm install
npm start

Features:
- AES-GCM encryption
- Multi-user approval
- File download with original format
- Modern UI + drag-drop + preview
